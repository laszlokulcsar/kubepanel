apiVersion: v1
data:
  custom-rules.conf: |
    {% autoescape off %}
    {% for line in rules.splitlines %}
    {{ line }}
    {% endfor %}
    {% endautoescape %}
kind: ConfigMap
metadata:
  name: modsecurity-custom-rules
  namespace: ingress
